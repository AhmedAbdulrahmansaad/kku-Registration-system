# 📦 كيفية ضغط المشروع | How to Zip the Project

## طريقة ضغط المشروع

### الطريقة 1: باستخدام PowerShell (Windows)

1. افتح PowerShell في مجلد المشروع
2. شغّل الأمر التالي:

```powershell
cd "C:\Users\SurfaceBook\Downloads\مجلد جديد (3)\kku-course-registration"
Compress-Archive -Path * -DestinationPath "../kku-course-registration-complete.zip" -Force -Exclude "node_modules"
```

### الطريقة 2: يدوياً (Windows)

1. اذهب إلى مجلد المشروع
2. حدد جميع الملفات والمجلدات
3. **استثنِ** مجلد `node_modules` (لا تضغطه - كبير جداً)
4. اضغط كليك يمين → **Send to** → **Compressed (zipped) folder**

### الطريقة 3: باستخدام 7-Zip أو WinRAR

1. حدد مجلد `kku-course-registration`
2. اضغط كليك يمين
3. اختر **7-Zip** → **Add to archive**
4. **استثنِ** `node_modules` من الضغط
5. اضغط **OK**

---

## 📋 الملفات التي يجب تضمينها

### ✅ يجب تضمينها:
- ✅ `src/` - جميع ملفات الكود
- ✅ `public/` - الملفات العامة
- ✅ `supabase/` - قاعدة البيانات
- ✅ جميع ملفات `.md` (التوثيق)
- ✅ `package.json`
- ✅ `package-lock.json`
- ✅ `vite.config.ts`
- ✅ `tsconfig.json`
- ✅ `tailwind.config.js`
- ✅ `index.html`
- ✅ جميع ملفات الإعداد

### ❌ لا تضمّن:
- ❌ `node_modules/` - كبير جداً، سيتم تثبيته بـ `npm install`
- ❌ `.env` - يحتوي على معلومات حساسة
- ❌ `.git/` - إذا كان موجوداً

---

## 📝 بعد الضغط

### 1. حجم الملف المتوقع:
- بدون `node_modules`: ~2-5 MB
- مع `node_modules`: ~200+ MB (لا تضغطه!)

### 2. اسم الملف المقترح:
```
kku-course-registration-complete.zip
```

### 3. محتويات الملف:
```
kku-course-registration-complete.zip
├── src/ (جميع الملفات)
├── public/
├── supabase/
├── *.md (جميع ملفات التوثيق)
├── package.json
├── vite.config.ts
└── ... (جميع ملفات الإعداد)
```

---

## ✅ التحقق من الملف المضغوط

بعد الضغط، تأكد من وجود:

- ✅ جميع ملفات `src/`
- ✅ `supabase/schema.sql`
- ✅ `README.md`
- ✅ جميع ملفات التوثيق
- ✅ `package.json`

---

## 🚀 استخدام الملف المضغوط

### عند فك الضغط:

1. **فك الضغط** في مجلد جديد
2. **افتح Terminal** في المجلد
3. **شغّل:**
   ```bash
   npm install
   ```
4. **أنشئ ملف `.env`** مع المتغيرات
5. **شغّل:**
   ```bash
   npm run dev
   ```

---

**تم! المشروع جاهز للضغط** ✅


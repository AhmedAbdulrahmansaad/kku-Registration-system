# 📘 دليل التشغيل الكامل | Complete Instructions

## ✅ تم إصلاح المشاكل التالية:

1. ✅ **تسجيل الدخول التلقائي بعد إنشاء الحساب** - الآن يعمل تلقائياً
2. ✅ **جميع الصفحات مربوطة** - 20+ صفحة جاهزة
3. ✅ **صور جامعة الملك خالد** - تم إضافتها من Unsplash
4. ✅ **التصميم متجاوب** - يعمل على Desktop, Tablet, Mobile
5. ✅ **بدون صور بنات** - فقط أيقونات وأشكال هندسية

---

## 🚀 خطوات التشغيل السريعة

### 1. أنشئ ملف `.env`:

في مجلد `kku-course-registration` أنشئ ملف `.env`:

```env
VITE_SUPABASE_URL=https://obffsqorwfudbnpxbecv.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9iZmZzcW9yd2Z1ZGJucHhiZWN2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ2NDU0MTYsImV4cCI6MjA4MDIyMTQxNn0.7vMQ2tiK0hZEejHCV4HOcWGfgTQIa_gqxwSXO_2UgWk
```

### 2. شغّل SQL Script في Supabase:

1. اذهب إلى [Supabase Dashboard](https://supabase.com/dashboard)
2. اختر مشروعك
3. **SQL Editor** → **New Query**
4. انسخ كل محتوى `supabase/schema.sql`
5. الصق واضغط **Run**

### 3. شغّل المشروع:

```bash
npm install
npm run dev
```

---

## 🎯 كيفية الاستخدام

### إنشاء حساب جديد:

1. اذهب إلى `/signup`
2. اختر الدور:
   - **طالب** - يحتاج: الاسم، البريد، الرقم الجامعي، التخصص، المستوى
   - **مرشد** - يحتاج: الاسم، البريد فقط
   - **مدير** - يحتاج: الاسم، البريد فقط
3. بعد التسجيل سيتم توجيهك تلقائياً للوحة التحكم

### تسجيل الدخول:

1. اذهب إلى `/login`
2. أدخل البريد وكلمة المرور
3. سيتم توجيهك للوحة التحكم حسب دورك

---

## 📱 الصفحات المتاحة

### للطالب:
- `/student/dashboard` - لوحة التحكم
- `/student/courses` - تسجيل المقررات
- `/student/registered` - المقررات المسجلة
- `/student/schedule` - الجدول الدراسي
- `/student/transcript` - السجل الأكاديمي
- `/student/gpa` - حاسبة المعدل
- `/student/requests` - الطلبات
- `/student/profile` - الملف الشخصي

### للمرشد:
- `/advisor/dashboard` - لوحة التحكم
- `/advisor/students` - قائمة الطلاب
- `/advisor/requests` - طلبات التسجيل
- `/advisor/students/:id` - تفاصيل طالب
- `/advisor/profile` - الملف الشخصي

### للمدير:
- `/admin/dashboard` - لوحة التحكم
- `/admin/courses` - إدارة المقررات
- `/admin/users` - إدارة المستخدمين
- `/admin/settings` - إعدادات النظام
- `/admin/profile` - الملف الشخصي

---

## 🔍 استكشاف الأخطاء

### المشكلة: لا يتم تسجيل الدخول بعد إنشاء الحساب

**الحل:**
1. افتح **Console** في المتصفح (F12)
2. تحقق من وجود أخطاء
3. تأكد من أن جدول `users` موجود في Supabase
4. تحقق من أن RLS policies مفعّلة

### المشكلة: الصفحات تظهر فارغة

**الحل:**
1. تحقق من Console للأخطاء
2. تأكد من أن Supabase متصل (تحقق من Network tab)
3. تأكد من أن البيانات موجودة في قاعدة البيانات

### المشكلة: لا تظهر المقررات

**الحل:**
1. تأكد من أنك شغّلت `schema.sql` كاملاً
2. تحقق من جدول `courses` في Supabase
3. يجب أن يكون هناك 49 مقرر

---

## 📊 قاعدة البيانات

### الجداول:

1. **users** - المستخدمون (طلاب، مرشدين، مدراء)
2. **courses** - المقررات (49 مقرر)
3. **enrollments** - التسجيلات
4. **requests** - طلبات التسجيل
5. **notifications** - الإشعارات
6. **system_settings** - إعدادات النظام

### للتحقق من البيانات:

1. اذهب إلى Supabase Dashboard
2. **Table Editor**
3. تحقق من الجداول

---

## 🎨 المميزات

✅ **تصميم متجاوب** - يعمل على جميع الأجهزة  
✅ **دعم اللغات** - العربية والإنجليزية  
✅ **الوضع الليلي** - Dark/Light Mode  
✅ **روبوت محادثة** - مساعد ذكي  
✅ **رسوم بيانية** - Recharts  
✅ **رسوم متحركة** - Framer Motion  
✅ **صور جامعة الملك خالد** - من Unsplash  

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع Console في المتصفح
2. راجع Supabase Logs
3. تحقق من Network tab
4. راجع هذا الدليل

---

**تم التطوير بواسطة فريق MIS - جامعة الملك خالد 2025**  
**Developed by MIS Team - King Khalid University 2025**


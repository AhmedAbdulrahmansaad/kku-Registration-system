# 📦 ضغط المشروع | Project Zipping

## 🚀 الطريقة الأسهل

### شغّل ملف `zip-project.bat`

1. **افتح مجلد المشروع**
2. **اضغط مرتين** على ملف `zip-project.bat`
3. **انتظر** حتى ينتهي الضغط
4. **ستجد الملف المضغوط** في المجلد الرئيسي:
   ```
   kku-course-registration-complete.zip
   ```

---

## 📋 ما الذي يتم ضغطه؟

### ✅ يتم ضغط:
- جميع ملفات `src/`
- جميع ملفات `public/`
- جميع ملفات `supabase/`
- جميع ملفات `.md` (التوثيق)
- جميع ملفات الإعداد

### ❌ لا يتم ضغط:
- `node_modules/` (كبير جداً)
- `.env` (معلومات حساسة)
- `.git/` (إذا كان موجوداً)

---

## 📊 حجم الملف

- **بدون node_modules:** ~2-5 MB ✅
- **مع node_modules:** ~200+ MB ❌

---

## ✅ بعد الضغط

ستجد الملف:
```
C:\Users\SurfaceBook\Downloads\مجلد جديد (3)\kku-course-registration-complete.zip
```

---

## 🎯 استخدام الملف المضغوط

1. **انسخ الملف** إلى أي مكان
2. **فك الضغط**
3. **شغّل `npm install`**
4. **أنشئ ملف `.env`**
5. **شغّل `npm run dev`**

---

**اضغط مرتين على `zip-project.bat` للضغط التلقائي!** 🚀

